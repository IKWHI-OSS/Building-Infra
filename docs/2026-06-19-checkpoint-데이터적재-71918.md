# 체크포인트 — 데이터 BK 적재 세션 · 2026-06-19 (재작성)

> 다음 세션 시작 시 이 블록을 붙여 깨끗한 컨텍스트에서 이어간다.
> 범위: **선정 데이터셋을 API 키로 받아 GCS BK에 적재**(무결성·1:1 크기검증·삭제 룰 포함)만.
> **71388 적재의 검증된 기계를 그대로 재사용한다 — 새로 만들지 말 것.**

---

[데이터 적재 — 2026-06-19]
부팅: CLAUDE.md → SOUL/LOOP/MEMORY/USER. 이어서 **필독**:
- `scripts/INGEST-AGENT.md` (적재 러너 정책 = 다운로드→무결성→업로드→크기검증→삭제 루프 + DIAGNOSIS 표 + 무한루프 방지 3원칙 + preflight + HITL)
- `scripts/aihub-71388-filekey-map.md`, `scripts/aihub-71388-label-schema.md` (형식 본보기)
- 러너: `scripts/aihub_ingest.sh`, `scripts/dl_to_gcs_loop.sh`(rm 전 업로드·검증 게이트 내장), `scripts/merge_parts.sh`

## 한 줄
선정 데이터셋을 API로 받아 BK 적재. 이 워크플로는 **자기검증 루프(Step→verify→DIAGNOSIS→유계재시도→차단기)** 구조라 인프라(orc/)와 같은 골격 — **인프라 인스턴스로 승격 대상**(승격은 인프라 세션 몫, 이 세션은 적재 실행).

## 적재 대상 (확정)
| dataSetSn | 이름 | 유형 | 용량 | 역할 |
|---|---|---|---|---|
| **71918** | 배터리 열폭주 멀티모달 | 열화상 이미지 + 5종 센서 시계열 + 텍스트 | 13.12GB | 핵심(모델 학습) |
| **71921** | 산불 확산 위험 대응방안 추론 | 텍스트(JSON, 자연어 Q&A 274,204건) | 1.86GB | RAG(확산 근거) |

- 보류: `71869` 리튬이온(물성 **수치·표**, RAG 부적합). 제외 권장: 산림 수종 이미지(전라57.5/경상86.5/제주10.1GB, 이미지·대용량·별도 비전모델).
- **물성 RAG**: AI Hub에 적합한 배터리 물성/안전 **텍스트 코퍼스 없음**(2026-06-19 브라우저 확인: 배터리화재·전기차화재·리튬안전 검색 모두 텍스트 문서 없음) → AI Hub 밖(규정·논문) 또는 보류.

## 0순위 — 가장 먼저: 물성·가연환경 데이터 선정 (외부 포함)
71918/71921은 확정. 그러나 **물성**(발화 근거)·**가연환경**(주변 확산 근거) 자료는 AI Hub에 적합한 게 없음(2026-06-19 브라우저 확인: 배터리화재·리튬안전·전기차화재 검색 = 텍스트 문서 없음; 산림수종은 이미지·대용량이라 제외 권장).
- **이 세션 첫 작업 = 에이전트가 AI Hub 밖(웹·KS 등 안전규정·논문 등)에서 물성·가연환경에 쓸 데이터셋 또는 대체품을 찾아 후보 제시 → 사용자는 검증만.**
- 적합 후보가 확정되면 BK 적재 대상에 추가(아래 같은 적재 루프 사용). 못 찾으면 물성/가연환경 RAG는 보류하고 71918 모델 + 71921로 진행.

## 1단계 — filekey 맵 (데이터셋당 1회만, 박제 후 재조회 금지)
71388처럼 `aihubshell -mode l -datasetkey <SN>` **1회 실측** → 파일트리·filekey를 아래 표에 박제(소형이라 키 수 적음). 이후 `-mode l` 재조회 금지(비용).
- 71918 filekey 맵: _(미실측 — `-mode l`로 채울 것)_
- 71921 filekey 맵: _(미실측 — `-mode l`로 채울 것)_
→ 채운 맵은 `scripts/aihub-71918-filekey-map.md`, `aihub-71921-filekey-map.md`로 저장.

## 1. 라벨/구조 스키마 (적재 무결성 + 후속 학습용)
각 데이터셋 상세의 "어노테이션 포맷 및 데이터 구조 / 메타데이터 구조표"를 1회 기록 → `scripts/aihub-<SN>-label-schema.md`로 박제(71388 스키마 문서 형식 따름).
- 71918: 열화상 이미지 + 센서(전압/전류/압력/온도/가스) 시계열, **열폭주 1~6단계 라벨**, 센서 이상이벤트.
- 71921: JSON, 자연어 질의응답(라벨링=질의응답 자연어).

## 2. 적재 루프 (INGEST-AGENT.md 그대로) — filekey 1개 = 1 트랜잭션
```
preflight(KEY/BK·gsutil 인증·디스크≥최대FK×2·aihubshell 패치확인) 1회 →
for FK in 큐:  DONE이면 skip
  download → unzip -t(무결성) → gsutil 업로드 → 로컬==GCS 크기 1:1 → OK면 로컬 삭제 → DONE/FAILED 기록
  단계 실패 = DIAGNOSIS 표로 (재시도?, 보정). 미진단 에러는 재시도 말고 FAILED+halt.
  연속 K회 FAILED → 전체 halt(systemic 의심, HITL)
```
- 상태: `~/aihub_dl/ingest_state.tsv`(filekey<TAB>STATUS, 멱등). 로그: `ingest_YYYYmmdd.jsonl`(감사).
- 성공 판정 = **0바이트 아닌 zip 존재**(로그 텍스트 스크래핑 금지). 에러 스캔은 zip 미생성 시에만.
- BK prefix 분리: `${BK}/aihub-71918/`, `${BK}/aihub-71921/`.

## 3. 리허설 (전량 전 정합성 확인 — 필수)
**샘플(경량) 데이터 또는 최소 1 filekey**로 `download→unzip -t→업로드→크기검증→삭제` 1사이클을 먼저 돌려 워크플로·스키마 정합성을 눈으로 확인(71388 brailleBlock 리허설과 동일 취지). 통과 후에만 전량.

## 주의 (MEMORY)
- **완료는 산출물(버킷 객체·용량)로 판정**, 로그 아님.
- 한글 파일명 merge 0바이트 버그(`printf %q`) — **패치본 aihubshell** 사용(재설치 시 패치 소실 주의).
- 다운로드는 **사용자 맥(한국 IP)**. 코워크 bash는 격리 리눅스라 맥 디스크/aihub 네트워크 직접 못 봄.
- HITL: IP/디스크/인증(gcloud auth) 문제, 연속 차단기, 미진단 에러 시 사람 호출.
